# Artifact XML3 Duration Checker

- `web/`: bản web portable, chạy bằng `python -m http.server 5173`.
- `single-page/xml3-duration-checker.html`: bản HTML offline, mở trực tiếp bằng trình duyệt.

Không đưa file XML nghiệp vụ thật vào thư mục release.
